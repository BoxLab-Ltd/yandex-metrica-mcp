# Authentication — choose ONE of the two modes below.
#
# Mode A (simplest): a static OAuth token. Obtain one for an app with the
# `metrika:read` scope at https://oauth.yandex.com and paste it here.
YANDEX_METRIKA_TOKEN=
#
# Mode B (interactive login + auto-refresh): register your own OAuth app at
# https://oauth.yandex.com (scope metrika:read), put its credentials here, then
# run `yandex-metrica-mcp auth` (or `bun run auth`) once. Tokens are cached at
# ~/.config/yandex-metrica-mcp/token.json (mode 0600) and refreshed automatically.
# YANDEX_OAUTH_CLIENT_ID=
# YANDEX_OAUTH_CLIENT_SECRET=

# Default counter id (optional). When set, tools may omit `counterId`.
# YANDEX_METRIKA_COUNTER_ID=

# Response language for human-readable labels: en | ru | tr | ... (default: en)
# YANDEX_METRIKA_LANG=en

# Override the token cache location (optional).
# YANDEX_METRIKA_TOKEN_FILE=

# API / OAuth base URLs (optional, only override for testing/mocking).
# YANDEX_METRIKA_BASE_URL=https://api-metrika.yandex.net
# YANDEX_OAUTH_BASE_URL=https://oauth.yandex.com
